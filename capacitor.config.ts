import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'checo.music.com',
  appName: 'checo-music',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
